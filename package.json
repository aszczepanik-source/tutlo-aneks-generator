{
  "name": "tutlo-aneks-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-release.mjs",
    "check": "npm run build && npm test && git diff --check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "docxtemplater": "3.62.0",
    "pizzip": "3.1.8"
  }
}
